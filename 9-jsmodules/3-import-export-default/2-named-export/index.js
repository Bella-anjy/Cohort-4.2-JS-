export const capitalizeString = (string) => {
  return string.toUpperCase();
}

export const addExclamation = (word) => {
  return `${word}!`;
}

export const addQuestionMark = (word) => {
  return `${word}!`;
}